// An enum representing the theme options.
export const themes = Object.freeze({
  DARK: "dark",
  LIGHT: "light",
});

// Has the theme been set successfully?
export const themeSet = Object.freeze({
  YES: "yes",
  NO: "no",
});

// The key to use with localStorage.
export const themeKey = "theme";
